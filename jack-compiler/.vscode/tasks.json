{
    "tasks": [
        {                                                      
            "type": "cppbuild",                                // When hitting ctrl+shift+b, this gets task run (assuming it's been set as the default build task).
            "label": "C/C++: g++-10 build active file",
            "command": "/usr/bin/cmake",                      
            "args": [
                "--build",
                "${workspaceFolder}/build"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "CMake"
        }
    ],
    "version": "2.0.0"
}